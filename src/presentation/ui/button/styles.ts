import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  base: {
    alignItems: 'center',
    borderRadius: 6,
    flexDirection: 'row',
    gap: 6,
    justifyContent: 'center',
  },
});
