import { StyleSheet } from 'react-native-unistyles';

export default StyleSheet.create({
  postListContainer: {
    paddingBottom: 24,
    paddingTop: 0,
  },
  userCard: {
    paddingHorizontal: 16,
  },
  userListContainer: {
    paddingBottom: 24,
    paddingTop: 0,
  },
});
